# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# Sensitive data
scripts/*.csv

# WhatsApp sender agent — never commit credentials or the WhatsApp session
whatsapp-sender/.env
whatsapp-sender/auth_state/
whatsapp-sender/sent_today.json
whatsapp-sender/node_modules

# WhatsApp gateway — never commit the service-role key or WhatsApp sessions
whatsapp-gateway/.env
whatsapp-gateway/sessions/
whatsapp-gateway/node_modules
